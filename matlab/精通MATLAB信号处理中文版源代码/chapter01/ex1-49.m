X=[0 -2 2]';Y=[1 2 2]';
C1=cov(X)     %X的协方差
C2=cov(X,Y)     %列向量X、Y的协方差矩阵，对角线元素为各列向量的方差
A=[1 2 3;4 0 -1;1 7 3]
C1=cov(A)    %求矩阵A的协方差矩阵
C2=var(A(:,1))    %求A的第1列向量的方差
C3=var(A(:,2))     %求A的第2列向量的方差
C4=var(A(:,3))
